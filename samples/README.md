# Samples

Fixture data used for manual/local testing of the scan pipeline — e.g. a
sample Figma node reference plus a matching local page to compare against.
Populated starting Phase 1 (Figma integration). Generated artifacts
(screenshots, diffs) are git-ignored; only small, hand-picked fixtures are
committed here.
